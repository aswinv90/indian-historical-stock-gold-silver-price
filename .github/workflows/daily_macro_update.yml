name: Daily Macroeconomic Indicators Update

on:
  schedule:
    # Runs every weekday at 13:05 UTC = 06:35 PM IST (opening the evening pipeline)
    - cron: '5 13 * * 1-5'

  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-macro:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Check if session was an Indian Market Working Day
        id: market_check
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "Manual trigger detected. Forcing update."
            echo "is_open=true" >> $GITHUB_OUTPUT
          else
            python scripts/check_market_day.py
          fi

      - name: Run daily macroeconomic indicators update
        if: steps.market_check.outputs.is_open == 'true'
        run: python scripts/update_macro.py

      - name: Commit and push updated macro data
        if: steps.market_check.outputs.is_open == 'true'
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/MACRO/
          if ! git diff --cached --quiet; then
            git commit -m "chore(macro): daily macroeconomic indicators update $(date -u +'%Y-%m-%d')"
            git pull --rebase origin main
            git push origin main
          else
            echo "No changes to commit for Macroeconomic Indicators."
          fi
